import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  dark: {
    color: 'white',
    fontSize: 20,
    fontWeight: 'bold',
  },
  white: {
    color: 'black',
    fontSize: 18,
    fontWeight: 'bold',
  },
});
export default styles;
