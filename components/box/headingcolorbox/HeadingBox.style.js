import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  box: {
    borderRadius: 3,
    padding: 5,
    alignItems: 'center',
    marginBottom: 10,
  },
});
export default styles;
